export const accessToken = localStorage.getItem('access_token') ?? '';
export const refreshToken = localStorage.getItem('refresh_token') ?? '';
export const memberId = Number(localStorage.getItem('member_id')) ?? '';
